using SymmetricCipher.Algorithms;
using SymmetricCipher.DataTest;
using System;
using System.Diagnostics;
using System.IO;

namespace SymmetricCipher
{
	class Program
	{
		static void Main(string[] args)
		{
			//FileEncryptor fileEncryptor = new FileEncryptor();
			//AES aes = new AES(KeyType.Small_128);
			//Stopwatch stopWatch = new Stopwatch();
			//fileEncryptor.EncryptFile(aes, "2b7e151628aed2a6abf7158809cf4f3c", stopWatch);
			//TimeSpan encryptTime = stopWatch.Elapsed;
			//Console.WriteLine("Encrypt time: " + String.Format("{0:00}:{1:00}:{2:00}.{3:00}", encryptTime.Hours, encryptTime.Minutes, encryptTime.Seconds, encryptTime.Milliseconds / 10));
			//stopWatch.Reset();
			//fileEncryptor.DecryptFile(aes, "2b7e151628aed2a6abf7158809cf4f3c", stopWatch);
			//TimeSpan decryptTime = stopWatch.Elapsed;
			//Console.WriteLine("Decypt time: " + String.Format("{0:00}:{1:00}:{2:00}.{3:00}", decryptTime.Hours, decryptTime.Minutes, decryptTime.Seconds, decryptTime.Milliseconds / 10));
		}
	}
}
